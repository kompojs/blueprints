import type { Metadata } from 'next'
import '@<%= it.scope %>/ui-<%= it.family %>/styles'
import { KompoWorkbench } from '@kompojs/workbench/next'
import { ServerComposition } from '../composition/ServerComposition'
import { ClientComposition } from '../composition/ClientComposition'

export const metadata: Metadata = {
  title: '<%= it.projectName %> | Kompo',
  description: 'A Kompo Web3 application',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body>
        <ServerComposition>
          <ClientComposition>{children}</ClientComposition>
        </ServerComposition>
        <KompoWorkbench />
      </body>
    </html>
  )
}
